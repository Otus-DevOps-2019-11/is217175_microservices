version: '3.3'
services:
  ui:
    volumes:
      - "../src/ui:/app"
    command: ["puma", "-w", "2", "--debug"]

  post:
    volumes:
      - "../src/post-py:/app"

  comment:
    volumes:
      - "../src/comment:/app"
    command: ["puma", "-w", "2", "--debug"]
